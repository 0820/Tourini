<?php
$con = mysql_connect("localhost", "root", "");
if (!$con)
{
	die('Couldn't connect server: ' . mysql_error());
}
$db_selected = mysql_select_db("tourini");
if (!$db_selected)
{
		die("Couldn't select DB : " . mysql_error());
}
?>